services:
  suno-prompt-gen:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: suno-prompt-gen
    restart: unless-stopped
    ports:
      - "13050:13050"
    env_file:
      - .env
